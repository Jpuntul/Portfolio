// UI Constants for consistent values across the application

// Scroll thresholds
export const SCROLL_THRESHOLD = {
  HEADER: 50,
  BACK_TO_TOP: 300,
};

// Card dimensions
export const CARD_DIMENSIONS = {
  FEATURED_MIN_WIDTH: "380px",
  FEATURED_MAX_WIDTH: "md", // Tailwind max-w-md
  FEATURED_HEIGHT: "430px",
  IMAGE_HEIGHT: "h-48",
};

// Animation delays (in seconds)
export const ANIMATION_DELAY = {
  FEATURED_CARD: 0.2,
  GRID_CARD: 0.1,
  CATEGORY_STAGGER: 0.2,
};

// Tech stack display limits
export const TECH_DISPLAY_LIMIT = {
  FEATURED: 3,
  GRID: 4,
};

// Typewriter effect speed (ms per character)
export const TYPEWRITER_SPEED = 100;

// Form submission timeout
export const FORM_TIMEOUT = 5000; // Clear status after 5 seconds
